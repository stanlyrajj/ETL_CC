# ResearchRAG — Python dependencies
# Install: pip install -r requirements.txt
# Then:    playwright install chromium

# ── Web framework ─────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
httpx==0.27.0
python-multipart==0.0.9

# ── Database ──────────────────────────────────────────────────
sqlalchemy==2.0.30
asyncpg==0.29.0

# ── Research paper APIs ───────────────────────────────────────
arxiv==2.1.3
requests==2.32.3

# ── PDF processing ────────────────────────────────────────────
pymupdf==1.24.4       # fitz — fast PDF text extraction
ftfy==6.2.0           # Unicode/encoding fixes

# ── NLP / Embeddings ─────────────────────────────────────────
sentence-transformers==3.0.1
torch==2.3.0          # CPU version — remove if using GPU build

# ── Vector store ──────────────────────────────────────────────
chromadb==0.5.3

# ── LLM — Google AI Studio (free tier) ───────────────────────
google-generativeai==0.7.2

# ── Carousel rendering ────────────────────────────────────────
playwright==1.45.0
reportlab==4.2.2
Pillow==10.4.0

# ── Utilities ────────────────────────────────────────────────
pydantic==2.7.4
python-dotenv==1.0.1